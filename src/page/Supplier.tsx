import React from 'react';
import Supplier from '../components/settings/Supplier';

const SupplierPage: React.FC<any> = (props) => {
    return <Supplier {...props} />;
};

export default SupplierPage;