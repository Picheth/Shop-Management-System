import React from 'react';
import Purchase from '../components/operations/Purchase';

const PurchasePage: React.FC<any> = (props) => {
    return <Purchase {...props} />;
};
export default PurchasePage;