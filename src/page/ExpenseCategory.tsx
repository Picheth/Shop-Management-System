import React from 'react';
import ExpenseCategory from '../components/settings/ExpenseCategory';

const ExpenseCategoryPage: React.FC<any> = (props) => {
    return <ExpenseCategory {...props} />;
};

export default ExpenseCategoryPage;