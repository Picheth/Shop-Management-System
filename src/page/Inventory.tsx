import React from 'react';
import Inventory from '../components/inventory/Inventory';

const InventoryPage: React.FC<any> = (props) => {
    return <Inventory {...props} />;
};
export default InventoryPage;